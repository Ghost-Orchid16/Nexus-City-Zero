{
  "name": "nexus-city-zero",
  "version": "1.0.0",
  "private": true,
  "description": "NEXUS: CITY ZERO — a 2D browser crisis-management game about saving a city before it collapses.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "art": "node tools/art/forge.mjs",
    "test": "node --test \"tests/unit/*.test.js\"",
    "test:e2e": "playwright test",
    "balance": "node tools/sim/balance.mjs",
    "styles": "node tools/sim/styles.mjs"
  },
  "dependencies": {
    "@fontsource/nunito": "^5.3.0",
    "@fontsource/pixelify-sans": "^5.3.0",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "vite": "^8.3.1"
  }
}
